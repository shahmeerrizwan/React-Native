import { StyleSheet } from "react-native"

export default ExStyle = StyleSheet.create({
    Externalstyle: {
        top: 90,
        textAlign: "center",
        fontWeight: "700",
        fontSize: 30,
        backgroundColor: 'orange',
        padding: 10,
    }
})
