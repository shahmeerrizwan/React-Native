import { StyleSheet } from "react-native"

export default ExStyle = StyleSheet.create({
    Externalstyle: {
        textAlign: "center",
        fontSize: 30,
        fontWeight: "700",
        backgroundColor: 'orange',
        padding: 10,
    }
})
